

limits on input + output trajectory lenght - sliding window vs cumulative / both up to a certain point

- Interval length
- Maximum acceptable "age" of a CO measurement (how many minutes can the last CO measurement be away before we replace it with a guess)
- What is the guessed CO value?
- By which criteria do we filter out patients with insufficient data?